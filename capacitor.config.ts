import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-nueva-plataforma-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
